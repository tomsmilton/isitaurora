name: Daily train report

on:
  # Aligned to UK summer time (BST, UTC+1): fires at 02, 04, 06, 08, 10, 15, 17, 19, 22 UK local.
  # In UK winter (GMT), these fire one hour earlier (01, 03, 05, 07, 09, 14, 16, 18, 21 local)
  # because cron runs in UTC and can't handle DST. Shift if you care.
  schedule:
    - cron: "0 1,3,5,7,9,14,16,18,21 * * *"
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Generate index.html
        env:
          RTT_API_TOKEN: ${{ secrets.RTT_API_TOKEN }}
        run: node generate.mjs

      - name: Commit updated report
        # Always publish, even if generate.mjs exited non-zero: the script still
        # writes a valid index.html (an empty/quiet-day page or an error page),
        # and committing it keeps the site fresh instead of letting it freeze on
        # the last good run. The job is still marked failed if generate failed.
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html data.json ledger.csv
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: daily report $(date -u +%Y-%m-%d)"
            git push
          fi
